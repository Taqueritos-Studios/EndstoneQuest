[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "endstone-quest"
version = "0.1.4"
description = "Quest plugin for Endstone"

[project.entry-points."endstone"]
quest = "quests:QuestPlugin"

[tool.hatch.build.targets.wheel]
packages = ["src/quests"]
